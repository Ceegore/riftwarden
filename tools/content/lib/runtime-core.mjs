import fs from "node:fs/promises"; import path from "node:path"; import { parseStrictJson } from "./strict-json.mjs"; import { sha256 } from "./manifest.mjs"; import { fail } from "./diagnostic.mjs";
function deepFreeze(v){ if(v&&typeof v==="object"&&!Object.isFrozen(v)){Object.freeze(v);for(const x of Object.values(v))deepFreeze(x);}return v; }
export async function loadVerifiedBundle(outDir){ const root=path.resolve(outDir); const manifest=parseStrictJson(await fs.readFile(path.join(root,"manifest.json"),"utf8"),"manifest.json"); const byType=new Map(); let total=0;
 for(const rec of manifest.files){ if(rec.path.includes("..")||rec.path.includes("/")||rec.path.includes("\\"))fail("P09_MANIFEST_HASH","Unsafe generated path",{sourcePath:rec.path}); const bytes=await fs.readFile(path.join(root,rec.path)); if(bytes.length!==rec.byteLength||sha256(bytes)!==rec.sha256)fail("P09_MANIFEST_HASH",`Hash mismatch ${rec.path}`,{sourcePath:rec.path}); const file=parseStrictJson(bytes.toString("utf8"),rec.path); if(file.generatedBy!=="riftwarden-content-compiler-v1"||file.schemaVersion!==manifest.schemaVersion||file.entityType!==rec.entityType)fail("P09_SCHEMA_TYPE","Generated header mismatch",{sourcePath:rec.path}); const map=new Map(); for(const entity of file.entities){if(map.has(entity.id))fail("P09_ID_COLLISION",`Runtime duplicate ${entity.id}`);map.set(entity.id,deepFreeze(structuredClone(entity)));} byType.set(rec.entityType,map); total+=map.size; }
 const countTotal=Object.values(manifest.counts).reduce((a,b)=>a+b,0); if(total!==countTotal)fail("P09_MANIFEST_COUNT","Runtime count mismatch"); return deepFreeze({manifest:deepFreeze(manifest),byType}); }
