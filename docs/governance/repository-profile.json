{
  "schemaVersion": 1,
  "mode": "migration",
  "expectedDefaultBranch": "main",
  "singleProject": true,
  "phase": "01",
  "allowDependenciesInPhase01": false,
  "notes": "For an existing repository, document the migration preflight separately. Gate G01 still requires the final single-project contract.",
  "migrationNote": "Phase 02 installed frozen dependencies; verify-root dependency/engine checks are expected as migration artifacts, not Phase 01 clean-bootstrap violations."
}
