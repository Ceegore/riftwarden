GENERATED_DIRECTORY_CONTRACT

# Content ledger

Machine-generated extraction workflow ledgers (imported from the Phase 10
starter-kit and maintained exclusively by the extraction tooling in
`tools/content/extraction/`). Files here are never hand-edited; they are
regenerated/validated by `content:ledger:counts`, `content:ledger:validate`,
`content:review:batches`, `content:extraction:release` and
`content:baseline:freeze`.
