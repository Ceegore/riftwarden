GENERATED_DIRECTORY_CONTRACT

# Authoritative source extracts

Hash-pinned authority inputs (GDD V5 and Development Plan V2 extracts) verified
against `inputs/PHASE10_INPUT_MANIFEST.json`. These are immutable inputs, never
hand-edited; any change invalidates the pinned SHA-256.
