name: PR

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

concurrency:
  group: pr-${{ github.event.pull_request.number }}-${{ github.workflow }}
  cancel-in-progress: true

env:
  CI: 'true'
  RIFTWARDEN_IMPLEMENTATION_PHASE: '17'

jobs:
  bootstrap_versions:
    name: PR / bootstrap-versions
    if: github.event.pull_request.draft == false
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: bootstrap-versions

  format_file_length:
    name: PR / format-file-length
    if: github.event.pull_request.draft == false
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: format-file-length

  lint_typecheck:
    name: PR / lint-typecheck
    if: github.event.pull_request.draft == false
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: lint-typecheck

  requirements_contracts:
    name: PR / requirements-contracts
    if: github.event.pull_request.draft == false
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: requirements-contracts

  unit_tests:
    name: PR / unit-tests
    if: github.event.pull_request.draft == false
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: unit-tests

  simulation_smoke:
    name: PR / simulation-smoke
    if: github.event.pull_request.draft == false
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: simulation-smoke

  integration_smoke:
    name: PR / integration-smoke
    if: github.event.pull_request.draft == false
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: integration-smoke

  web_build:
    name: PR / web-build
    if: github.event.pull_request.draft == false
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: web-build

  e2e_smoke:
    name: PR / e2e-smoke
    if: github.event.pull_request.draft == false
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: e2e-smoke

  workflow_security:
    name: PR / workflow-security
    if: github.event.pull_request.draft == false
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: workflow-security

  dependency_security:
    name: PR / dependency-security
    if: github.event.pull_request.draft == false
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: dependency-security

  localization_parity:
    name: PR / localization-parity
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_06_LOCALIZATION_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: localization-parity

  content_validate:
    name: PR / content-validate
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_09_CONTENT_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: content-validate

  content_reproducibility:
    name: PR / content-reproducibility
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_10_CONTENT_BUILD_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: content-reproducibility

  rules_gates:
    name: PR / rules-gates
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_11_RULES_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: rules-gates

  math_gates:
    name: PR / math-gates
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_12_MATH_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: math-gates

  random_gates:
    name: PR / random-gates
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_13_RANDOM_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: random-gates

  sim_gates:
    name: PR / sim-gates
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_14_SIM_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: sim-gates

  phase15_gates:
    name: PR / phase15-gates
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_15_MOVEMENT_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: phase15-gates

  phase16_gates:
    name: PR / phase16-gates
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_16_TARGETING_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: phase16-gates

  phase17_gates:
    name: PR / phase17-gates
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_17_COMBAT_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: phase17-gates

  crossruntime_gates:
    name: PR / crossruntime-gates
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_14_SIM_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: crossruntime-gates

  asset_manifest_validate:
    name: PR / asset-manifest-validate
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_38_ASSETS_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: asset-manifest-validate

  native_config_diff:
    name: PR / native-config-diff
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_04_NATIVE_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: native-config-diff

  android_debug_smoke:
    name: PR / android-debug-smoke
    if: github.event.pull_request.draft == false && vars.RIFTWARDEN_PHASE_04_NATIVE_ENABLED == 'true'
    uses: ./.github/workflows/reusable-node-gate.yml
    with:
      gate: android-debug-smoke
  required:
    name: PR / required
    if: always() && github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs:
      - bootstrap_versions
      - format_file_length
      - lint_typecheck
      - requirements_contracts
      - unit_tests
      - simulation_smoke
      - integration_smoke
      - web_build
      - e2e_smoke
      - workflow_security
      - dependency_security
      - localization_parity
      - content_validate
      - content_reproducibility
      - rules_gates
      - math_gates
      - random_gates
      - sim_gates
      - phase15_gates
      - phase16_gates
      - phase17_gates
      - crossruntime_gates
      - asset_manifest_validate
      - native_config_diff
      - android_debug_smoke
    steps:
      - name: Checkout result validator
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up exact Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .node-version
          package-manager-cache: false
      - name: Assert active and phased results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: node tools/ci/assert-needs-results.mjs --phase 17
